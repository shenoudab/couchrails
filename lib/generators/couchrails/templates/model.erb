class <%= class_name %> < <%= options[:parent] != nil ? options[:parent].classify : 'CouchRails::Document' %>
use_database '<%= class_name..underscore %>'
<% attributes.each do |attribute| -%>
  property :<%= attribute.declaration %>
<% end %>

  timestamps!

end
